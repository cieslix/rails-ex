<%= form_with(model: gomoku, local: true) do |form| %>
  <% if gomoku.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gomoku.errors.count, "error") %> prohibited this gomoku from being saved:</h2>

      <ul>
      <% gomoku.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :data %>
    <%= form.text_area :data, id: :gomoku_data, 'data-id' => @gomoku.id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
